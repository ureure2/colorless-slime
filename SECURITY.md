# 보안 정책

## 지원 범위

현재 보안 수정 대상은 최신 공개 버전인 `0.1.x`입니다.

## 취약점 신고

가능하면 GitHub의 비공개 보안 권고(Private vulnerability reporting)를 이용해 다음 내용을 보내 주세요.

- 영향을 받는 버전과 운영체제
- 재현 순서와 예상·실제 결과
- 영향 범위
- 필요한 경우 민감정보를 제거한 로그 또는 최소 예제

비공개 신고 기능을 사용할 수 없다면 공개 이슈에는 세부 공격 코드, 토큰, 개인 데이터와 배양 JSON을 올리지 말고 연락 방법만 남겨 주세요.

## 보안 경계

- 앱은 정적 화면을 Tauri WebView에서 실행하며 자체 원격 API를 호출하지 않습니다.
- Content Security Policy는 기본적으로 앱 자체 리소스만 허용합니다.
- 배양 데이터는 로컬 저장소에 있으며 암호화된 비밀 저장소가 아닙니다.
- 백업과 AI 분석 파일은 평문 JSON이므로 비밀번호나 민감정보 보관 용도로 사용하면 안 됩니다.
- Windows 설치 파일은 현재 코드 서명되지 않았습니다. 공식 GitHub 저장소 또는 Releases에서 받은 파일인지 확인하세요.

## 의존성 관리

공개 전 다음 검사를 기준으로 사용합니다.

```powershell
npm audit --audit-level=low
cargo audit --file src-tauri/Cargo.lock
npm run desktop:verify
```

자동 보안 수정에서 큰 버전 변경을 강제하는 경우에는 곧바로 적용하지 않고, 영향받는 도구와 전체 빌드를 함께 검증합니다.

현재 `cargo audit`은 알려진 취약점은 보고하지 않지만, Tauri의 크로스 플랫폼 잠금 의존성에 포함된 GTK3 및 일부 빌드 도구에 유지보수·건전성 경고를 표시합니다. 이 앱의 배포 대상인 Windows 빌드에는 GTK3가 컴파일되지 않습니다. Tauri와 잠금 의존성을 정기적으로 갱신하며 경고 변화를 확인합니다.
